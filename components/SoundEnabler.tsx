"use client";

// This component is invisible and just ensures sound interaction tracking is set up
// The actual sound trigger happens when user clicks the pig
export default function SoundEnabler() {
	// No visible UI - sound manager handles interaction detection
	return null;
}
